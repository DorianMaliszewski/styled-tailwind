import { TailwindPropertyType } from '.'

const tables: TailwindPropertyType[] = [
  ['tableBorder', 'border', ['collapse', 'separate']],
  [['tableLayout', 'table'], 'table', ['auto', 'fixed']],
]

export default tables
